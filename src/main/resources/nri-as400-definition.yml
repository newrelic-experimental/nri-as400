name: com.newrelic.as400
protocol_version: 1
description: Monitors iSeries (AS400) - jobs, message queues, system status, memory status, and disk usage
commands:
  job-list:
    command:
      - /usr/bin/java
      - -cp
      - ./bin/nri-as400.jar
      - -Dcom.ibm.as400.access.AS400.guiAvailable=false
      - com.newrelic.as400.GetJobList
    interval: 60
  message-queue:
    command:
      - /usr/bin/java
      - -cp
      - ./bin/nri-as400.jar
      - -Dcom.ibm.as400.access.AS400.guiAvailable=false
      - com.newrelic.as400.GetMsgQueue
    interval: 60
  system-status:
    command:
      - /usr/bin/java
      - -cp
      - ./bin/nri-as400.jar
      - -Dcom.ibm.as400.access.AS400.guiAvailable=false
      - com.newrelic.as400.GetSystemStatus
    interval: 60
  memory-status:
    command:
      - /usr/bin/java
      - -cp
      - ./bin/nri-as400.jar
      - -Dcom.ibm.as400.access.AS400.guiAvailable=false
      - com.newrelic.as400.GetMemoryStatus
    interval: 60
  disk-usage:
    command:
      - /usr/bin/java
      - -cp
      - ./bin/nri-as400.jar
      - -Dcom.ibm.as400.access.AS400.guiAvailable=false
      - com.newrelic.as400.GetDiskUsage
    interval: 60